on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'printing folder contetn'
        run: pwd

      #- name: 'test print'
      #  run: echo mostafa